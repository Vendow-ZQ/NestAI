# NestAI 技术现状 —— 大白话版

> 目标：让你一眼看懂现在的技术状态，知道"缺什么"和"先做什么"。

---

## 1. 一句话总结现状

**NestAI 现在是一个"外表完整、没有大脑"的 App。**

- 你能看到的 10 个页面，UI 全做完了，按钮能点、能滑、能跳转。
- 但所有内容都是"假数据"——就像售楼处的精装样板间，看着像真的，其实里面没有住人。
- 后端服务器跑起来了，但只回了你一句 "hello"，其他什么都不会干。

---

## 2. 技术栈是什么？（用比喻讲）

NestAI 分两层：**你看到的（前端）** 和 **你看不到的（后端）**。

### 前端 —— 你看到的界面

| 技术 | 通俗解释 | 为什么选它 |
|------|---------|-----------|
| **Taro** | 一个"翻译官"，你写一套代码，它能翻译成微信小程序、抖音小程序、手机网页（H5）三种样子 | 不用写三遍 |
| **React** | 搭积木的方式做界面，页面由一个个小组件拼起来 | 业界最主流 |
| **TypeScript** | 带类型检查的 JavaScript，写错会立刻报错 | 减少 bug |
| **TailwindCSS** | 原子化样式工具，直接写 `className="bg-red-500"` 就能变红 | 写样式很快 |
| **Zustand** | 一个轻量"记事本"，页面之间传数据用的 | 比 Redux 简单 |

### 后端 —— 你看不到的服务器

| 技术 | 通俗解释 | 现在状态 |
|------|---------|---------|
| **NestJS** | Node.js 的后端框架，帮你组织接口代码 | 已跑起来 |
| **PostgreSQL** | 数据库，存用户信息、方案、信件等 | 已装好，没连 |
| **Drizzle ORM** | 数据库的"翻译官"，用代码操作数据库，不用手写 SQL | 已装好，没写 |
| **Supabase** | 托管的 PostgreSQL 服务（含用户认证、实时订阅） | 已装好，没配 |
| **AWS S3** | 云上的硬盘，存用户上传的照片 | 已装好，没用 |
| **Zod** | 数据格式检查器，确保传过来的数据是对的 | 已装好，没用 |

**关键发现：** 后端像一间刚装修好的办公室——家具（依赖）全买好了，但还没通电、没装电话、没人上班。

---

## 3. 现在的数据从哪来？

### 真相：全部来自一个"假数据仓库"

文件位置：`src/lib/mock/data.ts`

里面硬编码了：
- 5 个空间场景模板
- 15 条干预方案（5 场景 × 3 档）
- 3 封示例信件
- 6 条 Feed 内容
- 5 组 Chat 选项
- 3 个 Next 动作

**无论你做什么操作，看到的都是这几条假数据。**

### 前端有 5 个"临时记事本"（Zustand Store）

| 记事本 | 记什么 | 刷新还在吗 |
|--------|--------|-----------|
| user-store | 用户有没有上传过空间、有没有走完一次闭环 | 不在 |
| space-store | 上传的照片路径、识别出的空间信息 | 不在 |
| lifestyle-store | Chat 问卷的答案 | 不在 |
| intervention-store | 干预方案、Next 收藏列表 | 不在 |
| memory-store | 生成的信件、历史干预记录 | 不在 |

**全是内存里的临时数据，刷新页面就清零。**

---

## 4. 前后端现在连上了吗？

**没有。**

前端写好了"发快递"的方法（`src/network.ts`），但从来没有发过任何快递。

```
┌──────────┐         ┌──────────┐         ┌──────────┐
│  你看到的 │   →     │ 前端代码  │   →     │  假数据   │
│  界面    │         │          │         │  文件     │
└──────────┘         └──────────┘         └──────────┘
                             │
                             │ 本该发请求给后端，但没发
                             ▼
                        ┌──────────┐
                        │  后端     │
                        │  localhost:3000
                        │  只有 hello/health
                        └──────────┘
```

---

## 5. 10 个页面现在是什么状态？

| 页面 | 你看到的 | 背后的真相 |
|------|---------|-----------|
| **Grow 首页** | 上传入口 + 下滑 Feed | Feed 是 6 条假数据循环 |
| **上传空间** | 能拍照、选图、多图预览 | 图片存在手机本地，没上传 |
| **Lifestyle Chat** | 三步问卷能选 | 答案只记在本地，没发给任何人 |
| **生成中** | 进度动画 | 固定 3 秒后自动跳走，没有真在计算 |
| **P3 干预方案** | 三档方案能切换 | 15 条写死的假方案 |
| **Next Tab** | 收藏列表能看 | 存在本地内存，刷新就没了 |
| **P5 新变化分享** | 拍照 + 写感受 | 点提交没发出去 |
| **P6 一封信** | 信 + 前后对比 | 3 封假信随机显示 |
| **Me Tab** | 我的空间/信件/历史 | 全是假数据 |
| **生成中（第2次）** | 更新记忆中 | 固定时长跳转 |

---

## 6. 后端现在有什么？

只有 2 个接口：

| 接口 | 返回 |
|------|------|
| `GET /api/hello` | `{ status: "success", data: "Hello World" }` |
| `GET /api/health` | `{ status: "success", data: "2026-05-13T..." }` |

就这些。没数据库、没存图片、没 AI、没用户系统。

---

## 7. 要让产品跑起来，后端需要补什么？

按产品主链路，后端需要做的接口：

### 最优先（P0）—— 核心链路必须通

| 接口 | 干什么 |
|------|--------|
| `POST /api/upload` | 接收用户照片，存到 S3 云盘，返回可访问的链接 |
| `POST /api/spaces/:id/analyze` | 把照片发给 AI（Vision），识别出空间类型、家具布局 |
| `POST /api/interventions/generate` | **接 LLM 的地方** —— 把空间信息 + 问卷答案传给大模型，生成三档干预方案 |
| `GET /api/interventions/:id` | 把生成的方案返回给前端展示 |

### 次要（P1）—— 闭环必须通

| 接口 | 干什么 |
|------|--------|
| `POST /api/chat/sessions` | 保存 Lifestyle Chat 的问卷数据 |
| `POST /api/next` | 用户点"今晚试试看"，把动作存进数据库 |
| `PATCH /api/next/:id/complete` | 用户说"我做了"，标记完成 |
| `POST /api/feedback` | 接收反馈照片 + 文字感受 |
| `POST /api/letters/generate` | **接 LLM 的地方** —— 根据反馈生成一封信 |
| `GET /api/letters` | 获取信件列表 |
| `POST /api/auth/login` | 微信小程序登录，拿 openid |

### 锦上添花（P2）—— 社群功能

| 接口 | 干什么 |
|------|--------|
| `GET /api/feed` | 推荐流（看别人怎么改造的） |
| `POST /api/feed/:id/collect` | 收藏别人的 Next |

---

## 8. 数据库要存什么？

简单来说，要存 7 种东西：

```
用户 (users)
  └── 空间 (spaces) —— 用户上传的照片、AI识别结果
        └── 问卷会话 (chatSessions) —— Lifestyle Chat 的答案
              └── 干预方案 (interventions) —— 三档方案
                    └── Next 动作 (nextActions) —— 用户收藏的执行项
                          └── 反馈 (feedbacks) —— 用户拍的变化照片和感受
                                └── 信件 (letters) —— AI 生成的信

Feed 内容 (feedItems) —— 可分享的改造故事
```

---

## 9. 三个 LLM / LangGraph 接入点

将来接真实 AI 时，只需要改这三个地方：

| 接入点 | 做什么 | 输入 | 输出 |
|--------|--------|------|------|
| **空间分析** | 看懂用户上传的照片 | 空间照片 | 空间类型、家具列表、可干预点 |
| **方案生成** | 写三档改造方案 | 空间信息 + 问卷答案 | 三档方案（文字 + 购物清单） |
| **信件生成** | 写一封温柔的信 | 用户反馈（照片 + 感受） | 一封信（多段文字） |

**这三个点将来可以用 LangGraph 做成"状态机工作流"**，比如：
- 空间分析 → 判断空间类型 → 决定问什么问题 → 收集答案 → 生成方案 → 用户选择 → 生成信件

---

## 10. 如果明天开始开发，第一步做什么？

不要一口气全做。按这个顺序：

### 第 1 步：让图片能上传（前后端都碰）
- 前端：拍完照后，调用 `Network.uploadFile` 发给后端
- 后端：接收图片，上传到 S3，把 URL 存进数据库
- 验证：上传后刷新页面，图片还在

### 第 2 步：让方案能生成（先接 Mock API）
- 后端：写一个 `POST /api/interventions/generate` 接口
- 先不要接真 LLM，返回写死的 Mock 数据即可
- 前端：把 `MOCK_INTERVENTIONS` 的调用改成发请求
- 验证：前端能看到"从服务器返回"的方案

### 第 3 步：让 Chat 数据能保存
- 后端：建 `chatSessions` 表，写接口
- 前端：问卷答完后，把数据发过去
- 验证：换手机登录，还能看到之前的答案

### 第 4 步：让 Next 能持久化
- 后端：建 `nextActions` 表，写增删改查接口
- 前端：点"今晚试试看"时发请求保存
- 验证：刷新页面，Next 还在

### 第 5 步：让反馈和信件跑通
- 后端：写反馈接口 + 信件生成接口（先返回 Mock 信件）
- 前端：P5 提交后真发到服务器，P6 从服务器取信件
- 验证：走完一次完整闭环

### 第 6 步：接真 LLM（LangGraph）
- 把方案生成和信件生成里的 Mock 数据，替换成真实调用
- 这时候才需要搞 LangGraph 状态机、Prompt 工程、图像模型

---

## 11. 关键数字速查

| 项目 | 数字 |
|------|------|
| 前端页面 | 10 个 |
| 前端 Store | 5 个 |
| 假数据条目 | 干预方案 15 + 信件 3 + Feed 6 = 24 条 |
| 后端可用接口 | 2 个（hello / health） |
| 后端已装但未使用依赖 | Drizzle ORM、Supabase、AWS S3、pg、Zod = 5 个 |
| 数据库需要表 | 7 张 |
| LLM 接入点 | 3 个（空间分析 / 方案生成 / 信件生成） |

---

## 12. 相关文档

- [NestAI Product Definition v0.5](NestAI_Product_Definition_v0.5.md) —— 产品要做成什么样
- [Frontend.md](Frontend.md) —— 前端详细技术文档（偏技术细节）
