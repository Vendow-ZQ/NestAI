# NestAI 前后端数据联调方案

## 数据流转链路

```
用户上传图片
    ↓
后端视觉分析 (GPT-4o + Prompt1.md)
    ↓
生成 Memory01.md (人格洞察档案)
生成 QA.md (深度验证问卷)
    ↓
QA转换器 → 前端格式 {q, options[]}
    ↓
前端ChatPage展示问卷
    ↓
用户回答 → 后端整合 → Prompt2.md
    ↓
生成 Memory02.md (人格化干预方案)
    ↓
前端ResultPage展示三层改造方案
```

## 核心数据格式

### 1. 后端 → 前端 (GET /api/sessions/{id})

```json
{
  "success": true,
  "data": {
    "sessionId": "uuid",
    "spaceId": "uuid",
    "status": "analyzed",
    "shortTermMemory": "markdown内容",
    "spaceAnalysis": {
      "summary": "人格洞察档案 (Memory01)",
      "questions": [
        {"q": "问题1", "options": ["A", "B", "C"]},
        {"q": "问题2", "options": ["A", "B", "C"]}
      ],
      "qa_markdown": "完整QA.md内容",
      "personality_insights": {
        "dominant_personality": "控制型极简主义者",
        "lifestyle_prototype": "数字游民",
        "unmet_needs": ["创造力激发", "社交连接"],
        "key_contradictions": ["追求高效但缺乏灵感"]
      },
      "images": ["/uploads/xxx.jpg"]
    },
    "interventionPlan": {
      "tonight": {...},
      "this_week": {...},
      "this_month": {...}
    }
  }
}
```

### 2. 前端问卷格式

```typescript
questions: Array<{
  q: string;        // 问题文本
  options: string[]; // 选项数组
}>
```

### 3. 用户回答格式

```json
{
  "answers": [
    {"question_id": 1, "selected": "A", "text": "功能性和高效"},
    {"question_id": 2, "selected": "B", "text": "丰富而多样"}
  ]
}
```

### 4. QA.md 完整格式 (后端存储)

```markdown
## 问题1：[验证主导人格特质]
**我们想验证的推断**：用户是控制型人格
**观察证据**：
- 书桌上几乎无物
- 数据线捆扎整齐
**问题**：当你焦虑时最可能做？
**选项**：
- A. 立刻整理房间
- B. 找朋友聊天
- C. 专注工作
- D. 吃东西
**设计意图**：确认人格以调整干预策略
```

## 前端适配建议

### ChatPage 增强

1. **展示人格洞察** (可选):
```typescript
// 从API获取personalityInsights
const personality = data.personalityInsights;
// 在页面上展示："我观察到你是【控制型人格】..."
```

2. **问题展示保持不变**:
```typescript
// 原有代码完全兼容
questions: Array<{q: string, options: string[]}>
```

3. **回答收集增强**:
```typescript
// 收集答案时同时记录question_id
const answers = questions.map((q, idx) => ({
  question_id: idx + 1,
  selected: selectedOption,
  text: selectedText
}));
```

### ResultPage 增强

接收干预方案数据:
```typescript
interventionPlan: {
  tonight: {
    title: "",
    description: "",
    steps: [],
    psychology_principle: "" // 新增：心理学原理
  }
}
```

## 后端API端点

| 端点 | 功能 | 请求体 | 响应 |
|-----|------|-------|------|
| `POST /api/upload` | 上传图片 | FormData(images) | `{urls: []}` |
| `POST /api/spaces` | 创建空间 | `{images: []}` | `{id: ""}` |
| `POST /api/sessions` | 创建会话 | `{spaceId, images: []}` | `{id: ""}` |
| `POST /api/sessions/{id}/analyze` | 分析空间 | - | `{spaceSummary, questions}` |
| `GET /api/sessions/{id}` | 获取详情 | - | 完整数据(见上方) |
| `POST /api/sessions/{id}/intervention` | 生成方案 | `{answers: []}` | `{plan}` |

## 测试验证步骤

1. **放置测试图片**:
   ```bash
   cp your_room.jpg api_test/pic/Pic1.jpg
   cp your_room2.jpg api_test/pic/Pic2.jpg
   ```

2. **运行本地测试**:
   ```bash
   cd api_test
   python test_vision_v2.py  # 生成 Memory01 和 QA
   python test_intervention.py  # 生成 Memory02
   ```

3. **Web App测试**:
   - 打开 http://localhost:5000
   - 上传图片
   - 检查Network面板看API返回
   - 确认questions格式正确
   - 回答后生成干预方案

## 关键优势

✅ **后端**: 使用人格化Prompt深度分析，生成Memory01+QA  
✅ **转换**: QAConverter自动转换为前端格式  
✅ **前端**: 无需修改数据结构，完全兼容  
✅ **增强**: 可选展示personalityInsights增加"哇"时刻  

## 下一步

1. 前端可选：展示人格洞察卡片("我看出你是...")
2. 前端增强：收集答案时发送到后端
3. 后端增强：整合答案生成Memory02
4. 前后端联调：完整测试P001→P002→P003链路